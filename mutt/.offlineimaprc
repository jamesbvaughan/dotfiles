[general]
accounts = main
pythonfile = ~/.offlineimap.py
synclabels = yes
ui = basic
socktimeout = 45

[Account main]
localrepository = main-local
remoterepository = main-remote

[Repository main-local]
type = GmailMaildir
localfolders = ~/Mail
nametrans = translate_folder

[Repository main-remote]
type = Gmail
remoteuser = yoyovaughan@gmail.com
remotepasseval = get_pass()
nametrans = untranslate_folder
sslcacertfile = /etc/ssl/certs/ca-certificates.crt

[mbnames]
enabled = no
filename = ~/.mutt/mailboxes
header = "mailboxes "
peritem = "+%(foldername)s"
sep = " "
footer = "\n"

# vim: ft=dosini
