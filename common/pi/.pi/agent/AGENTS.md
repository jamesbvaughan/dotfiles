# Global Agent Rules

- Never run `git commit` or `git push` (or otherwise rewrite/publish git history) unless explicitly asked to in the current conversation. Read-only git commands (`git status`, `git diff`, `git log`, etc.) are fine anytime.
