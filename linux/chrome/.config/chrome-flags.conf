# Read by the /usr/bin/google-chrome-stable wrapper and passed to Chrome.
# Lines starting with # are stripped by the wrapper, so comments are safe here.

# Use GTK 4 for Chrome's own dialogs (file pickers and the like) so they match
# the GTK 4 / libadwaita apps rather than looking a decade older.
--gtk-version=4
