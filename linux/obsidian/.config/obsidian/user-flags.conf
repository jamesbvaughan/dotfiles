# Read by the /usr/bin/obsidian wrapper and passed to Electron.

# Run natively on Wayland instead of going through XWayland.
# Electron 43 still defaults to the X11 backend, which also means Obsidian dies
# on startup when launched from something without DISPLAY set, like vicinae.
--ozone-platform=wayland
